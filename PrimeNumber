number = int(input("Enter number to check if it's a prime number: "))

count = 0

for i in range(1, number + 1):
    if number % i == 0:
        count += 1

if count == 2:
    print(f"{number} is a Prime Number")
else:
    print(f"{number} is NOT a Prime Number")
